.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# runtime state: db, raw attachments, chroma index
var/

# fixtures are generated deterministically by evals/fixtures/generate.py,
# so the generator stays the source of truth rather than the binaries
evals/fixtures/*.pdf

.env
.DS_Store
evals/corpus/
