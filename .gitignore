.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# runtime state: db, raw attachments, chroma index
var/

# rendered PDFs are derived deterministically by evals/render.py,
# so the generator stays the source of truth rather than the binaries
evals/documents/rendered/

.env
.DS_Store
evals/documents/collected/

# Written by `make coverage`; the floor is enforced, the artefact is not kept.
.coverage
